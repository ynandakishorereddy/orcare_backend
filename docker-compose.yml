version: '3.8'

services:
  api:
    build: .
    container_name: orcare-backend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
    env_file:
      - .env
    restart: unless-stopped
